/* global: ClassySlider */
(function () {
  var opts = {
    el: document.getElementsByTagName('ul')[3],
    timer: 2500,
    transition: 'slide-left'
  };
  
  var slider = new ClassySlider(opts);
}());
